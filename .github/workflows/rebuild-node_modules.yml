name: rebuild-node_modules
on:
  pull_request:
    branches:
      - deploy
    paths:
      - 'packages/express/package.json'
      - 'packages/express/yarn.lock'
jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this rebuild script is running"
